(http://irreal.org/blog/?p=5218)
(http://mbork.pl/2016-05-15_debug-on-whatever)

(kmax should attempt to merge of a file on disk that has changed
 when the buffer has also changed, instead of simply asking which
 one to keep)

(see kmax/notes)

(the kmax-command-log should be capable of scrolling up to view
 it instead of jumping to the end. should use save-excursion or
 something to jump to the end.)

(describe-syntax for situated emacs agent)

(save all macros we record with emacs to a database, and have the
 ability to search and replay those macros.  use it as a case
 base too)
